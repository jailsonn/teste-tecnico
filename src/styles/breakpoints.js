// Braakpoints, responsividades, variávesis css

const size = {
    sm: '515px',
    md: '790px',
    bg: '1045px'
}

const breakpoints = {
    sm: `(max-width: ${size.sm})`,
    md: `(max-width: ${size.md})`,
    bg: `(max-width: ${size.bg})`,
}

export default breakpoints